{
  "name": "timetrack",
  "version": "1.0.0",
  "description": "Time tracking system with MS SQL Server backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node -e \"require('./seed').seedDatabase().then(() => process.exit(0)).catch(err => { console.error(err); process.exit(1); })\"",
    "seed-admin": "node -e \"require('./seed').seedAdminUser().then(() => process.exit(0)).catch(err => { console.error(err); process.exit(1); })\"",
    "reset-admin-password": "node scripts/reset-admin-password.js",
    "reset-all-passwords": "node scripts/reset-all-passwords.js"
  },
  "keywords": [
    "timetracking",
    "mssql",
    "punch-clock"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mssql": "^10.0.1",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "bcrypt": "^5.1.1",
    "express-session": "^1.17.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}

